{
  "version": "0.2.0",
  "configurations": [
    
    {
      "name": "(lldb) Launch",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/zig-out/bin/Cowsay",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build",
    }
  ]
}
